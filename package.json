{
  "name": "tradedashboard-seasonality",
  "private": true,
  "scripts": {
    "build:seasonality": "esbuild src/main.tsx --bundle --minify --format=esm --target=es2020 --outfile=assets/seasonality-app.js",
    "test:schwab-worker": "node worker/test.mjs"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "recharts": "^3.1.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.8"
  }
}
